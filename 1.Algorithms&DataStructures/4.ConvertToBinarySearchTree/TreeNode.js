/**
 * Definition for a binary tree node.
 * @typedef TreeNode {{ val: *, left: TreeNode, right: TreeNode }}
 */
class TreeNode {
    constructor(val) {
        this.val = val;
        this.left = this.right = null;
    }
}

/**
 * Convert array to binary search tree.
 * @param {number[]} arr
 * @return {TreeNode}
 */
const convertToBST = (arr) => {
};

/**
 * Print BST.
 * Example: [0,-3,9,-9,null,4]
 * Bonus: it will be great if program print tree like:
 *     0
 *    / \
 *  -3   9
 *  /   /
 * -9  4
 * @param {TreeNode} tree
 * @return {void}
 */
const printBST = (tree) => {
};
